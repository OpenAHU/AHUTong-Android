# mainpage.md — 主页"类夸克"设计思路

> 目标：把安大通主页从"卡片堆叠"改造成夸克浏览器式的**干净启动器**，
> 摆脱纯文字卡片的枯燥感。本文档作为设计蓝图，供后续统一实施参考。
>
> 状态：**设计蓝图中（暂未编码）**。等用户确认后按本文案实施。

---

## 1. 对标参考（夸克主页）

- **整体气质**：大面积留白、内容居中、极简"启动器式"首页，几乎无多余装饰。
- **顶部**：品牌 Logo + 小徽标作视觉锚点；可选页面圆点（支持横滑多页）。
- **中部 Hero 卡**：一张圆角大卡（搜索框卡：占位文案 + 搜索按钮 + 语音/相机/加号图标），是全页唯一大卡、视觉重心。
- **卡片下方**：功能入口**图标网格**，典型一行 5 个，每格 = 彩色圆角图标 + 下方一行小字。
- **底部**：4 个 tab 导航栏。

---

## 2. 落地到安都通主页的映射

### 2.1 Hero 卡位 = 校园卡
- 把夸克"搜索框卡"的位置换成**校园卡**。
- 保留现有效果：**液态玻璃**卡片 + 点击**展开二维码**的圆角矩形拉伸-收缩动画。
- 它成为主页唯一大卡、视觉重心，通栏圆角卡，宽度与夸克搜索卡相仿。

### 2.2 功能入口 = 图标 + 小文字网格
- 把现有 8 个"纯文字小卡片"改成夸克式图标网格。
- 每格 = **彩色圆角图标 + 小字标签**。
- 入口：浴室、电费、成绩、考试、课表、电话本 等。
- 布局：一行 4~5 个。

### 2.3 整体氛围
- 更多留白、更轻的视觉重量。
- 首页从"卡片堆叠"变成"干净的启动器"。

---

## 3. 可复用的现有资产（保持不变）

- **插槽自定义机制**：现有"长按拖拽 + 小工具库"直接作用于图标网格——拖的是**图标入口**而非文字卡。
- **校园卡的液态玻璃 + 展开动画**：保留。
- **图标设计语言**：复用设置页行图标那套（圆角色块底 + 线性图标），与全 App 设计语言统一，不另造彩色拟物图标。

### 保留的插槽架构（来自 Home 实现）
- 8 个逻辑插槽 `HomeWidgetSlotLayout`：第一行 = 左侧校园卡横卡 + 右侧两竖槽（66dp）；后三行 `(3|4)(5|6)(7|8)` 两两一行（76dp、各占 1f）。
- 所有小卡片共用唯一渲染组件 `TextHomeWidgetCard`（一处改动即覆盖全部插槽）。
- 添加流程：底部抽屉 `HomeWidgetLibrarySheet` → 点击/长按拖拽进空插槽 → `HomeWidgetDragOverlay` 拖拽预览。

---

## 4. 与此前液态玻璃体系的关系

- 本次主页改造基于已完成的液态玻璃体系（`GlassBackdropContainer` / `liquidGlassSurface` / `LocalIsLiquidGlassEnabled`）。
- 图标入口随液态开关实时切换风格；图标换色能力依赖 **SVG → VectorDrawable + tint**（见开发计划 5.2）。

---

## 5. 配套改造（待确认范围）

### 5.1 主页图标重构（配合类夸克改造）
- 用 **iconpark 线性/单色 SVG** 统一替换主页功能入口图标。
- 经 Android Studio **Vector Asset** 转成 `res/drawable/xxx.xml`，Compose 用 `painterResource` 并支持 `tint` 随主题。
- 现有 Material `Icons.*` 可一并换成统一线性风格，形成全 App 一致图标语言。
- **不要用 PNG/Vue 代码**（详见下方决策备注）。

### 5.2 图标替换决策（已和用户确认原则）
| 方案 | 结论 |
|------|------|
| **SVG → VectorDrawable/ImageVector** | ✅ 推荐：无损缩放、可 `tint` 随主题、体积小、与现有 21 个 vector 统一 |
| PNG 位图 | ❌ 放大糊、需多密度、不能动态换色、违背"随开关同步"设计 |
| iconpark "Vue 代码"导出 | ❌ 本项目为 Android Compose，不存在 Vue，忽略 |

---

## 6. 待办 / 后续步骤

- [ ] 用户下载好 iconpark SVG（线性/单色最优），放入本地指定目录。
- [ ] 校验全部 SVG 可由 Vector Drawable 支持。
- [ ] Vector Asset 批量转换 → `res/drawable/*.xml`。
- [ ] 替换 Compose 中对应 `Icons.*` / `painterResource` 引用。
- [ ] 按本蓝图落地主页「校园卡 Hero + 图标网格」布局。
- [ ] 构建安装到真机验证。

---

## 附：用户设计偏好（常态约束，UI 优化需遵守）
- 简洁、美观、成熟；标题栏纯导航固定顶部，搜索/筛选在可滚动区。
- 三枚极简 logo 按钮（搜索/筛选/更多-下拉），按钮等距（参考成绩单页三键间隔 8）。
- 按钮边距、文字大小等设计语言统一；注重运行动画规范。
- 偏好隐藏技术细节，倾向自动化的稳定流程，先保证核心功能。
- 允许大胆重构；倾向官方/现成组件；问题用简单直接方案解决。